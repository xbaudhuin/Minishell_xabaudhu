
character special () && || | $ * "" '' $? >> > << < 'espace outside quotes'
dollar seul = '$'

early_error: start with &&, ||, |, & = control operator; (0 command before)

error: () :	-double parenthesis empty,
			-opening_flag not closed, close_flag before corresponding opening flag
			-"command1 control_op empty"= no command after control_op
			-redirection_op not followed by string
			-

()			=	at least 1 command
("''")		must be properly imbricated:	-must not be crossed: "'"' !!!
											-most_out quote = main_quote till next main_quote
									
prompt: trim_space-> check first char:	-if parenthesis_open ->flag++;
										-if parenthesis_close && flag_open== 0->error
										-if " or ' -> flag++;
										-if operator && flag_operator == TRUE->error
										-if redirection-> flag++;

										if (not_special char -> operator == FALSE ->i++)




(echo "salut" && touch "salut") -> token echo ->inside_token 
